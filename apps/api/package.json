{
  "name": "@falcon/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "lint": "eslint src",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
        "@falcon/permission-engine": "workspace:*",
        "argon2": "^0.45.1"
       },
       "devDependencies": {
            "@types/node": "24.10.13",
            "@testcontainers/postgresql": "^12.0.4",
            "postgres": "3.4.7",
            "testcontainers": "^12.0.4"
           }
}
